import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import{FiDownload,FiShield}from'react-icons/fi';import toast from'react-hot-toast';import api from'../services/api';import PageHead from'../components/common/PageHead';
export default function VisitorPass(){const{id}=useParams(),[pass,setPass]=useState(null);useEffect(()=>{api.get(`/visitors/${id}/pass`).then(r=>setPass(r.data)).catch(e=>toast.error(e.message))},[id]);const download=async()=>{try{const r=await api.get(`/visitors/${id}/pass/pdf`,{responseType:'blob'}),url=URL.createObjectURL(r),a=document.createElement('a');a.href=url;a.download=`${pass.passNumber}.pdf`;a.click();URL.revokeObjectURL(url)}catch(e){toast.error(e.message)}};if(!pass)return <div className="page-loader"/>;return <main className="page"><PageHead eyebrow="Secure visitor credential" title={pass.passNumber} description="Approved visitor pass. The QR reference can be verified without exposing private identity details."/><article className="visitor-form password-form"><section className="form-section"><div className="section-number"><FiShield/></div><div className="section-copy"><h2>{pass.visitorName}</h2><p>{pass.companyName||'Independent visitor'}</p></div><div className="form-grid"><Info l="Host" v={pass.employee?.name}/><Info l="Department" v={pass.department?.name}/><Info l="Visit date" v={new Date(pass.visitDate).toLocaleDateString()}/><Info l="Schedule" v={`${pass.expectedArrival} - ${pass.expectedDeparture}`}/><Info l="Purpose" v={pass.purpose}/><Info l="Status" v={pass.status.replace('_',' ')}/></div></section><footer className="form-footer"><span>Receptionists, the assigned employee, and administrators may access this pass.</span><button className="primary" onClick={download}><FiDownload/>Download PDF</button></footer></article></main>}function Info({l,v}){return <div className="field"><span>{l}</span><strong>{v||'-'}</strong></div>}
