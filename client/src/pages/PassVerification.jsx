import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import api from'../services/api';
export default function PassVerification(){const{code}=useParams(),[data,setData]=useState(),[error,setError]=useState('');useEffect(()=>{api.get(`/passes/verify/${code}`).then(r=>setData(r.data)).catch(e=>setError(e.message))},[code]);return <main className="login-page"><section className="login-card"><div className="brand-mark">VP</div><h1>{error?'Pass not verified':data?'Visitor pass verified':'Verifying pass...'}</h1>{error?<p>{error}</p>:data&&<><p>{data.passNumber}</p><dl><dt>Visitor</dt><dd>{data.visitorName}</dd><dt>Host</dt><dd>{data.host}</dd><dt>Visit date</dt><dd>{new Date(data.visitDate).toLocaleDateString()}</dd><dt>Status</dt><dd>{data.status.replace('_',' ')}</dd></dl></>}</section></main>}
