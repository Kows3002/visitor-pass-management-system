const AppError=require('../utils/appError');const unsafe=value=>{if(!value||typeof value!=='object')return false;return Object.entries(value).some(([key,item])=>key.startsWith('$')||key.includes('.')||unsafe(item))};module.exports=(req,_res,next)=>unsafe(req.body)||unsafe(req.query)||unsafe(req.params)?next(new AppError('Request contains prohibited input',400,'UNSAFE_INPUT')):next();
