const attempts=new Map();module.exports=({windowMs=900000,max=10}={})=>(req,res,next)=>{const key=req.ip,now=Date.now(),entry=attempts.get(key);if(!entry||entry.reset<=now){attempts.set(key,{count:1,reset:now+windowMs});return next()}entry.count+=1;if(entry.count>max){res.set('Retry-After',Math.ceil((entry.reset-now)/1000));return res.status(429).json({success:false,message:'Too many attempts. Please try again later.'})}next()};
