const Employee=require('../models/Employee');const User=require('../models/User');const links=require('../services/employeeLinkService');const AppError=require('../utils/appError');const{ok,created}=require('../utils/response');const escape=value=>value.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
exports.list=async(req,res,next)=>{try{const page=Math.max(+req.query.page||1,1),limit=Math.min(+req.query.limit||10,100),q={};if(req.query.status)q.status=req.query.status;if(req.query.department)q.department=req.query.department;if(req.query.unlinked==='true'){const unlinked={$and:[{$or:[{userId:null},{userId:{$exists:false}}]},{$or:[{user:null},{user:{$exists:false}}]}]};q.$and=[req.query.include?{$or:[unlinked,{_id:req.query.include}]}:unlinked]}if(req.query.search){const search={$or:['fullName','email','phone','designation'].map(key=>({[key]:{$regex:escape(req.query.search),$options:'i'}}))};q.$and=[...(q.$and||[]),search]}const allowed=['fullName','email','designation','status','createdAt'],sortBy=allowed.includes(req.query.sortBy)?req.query.sortBy:'createdAt',sortOrder=req.query.sortOrder==='asc'?1:-1;const[items,total]=await Promise.all([Employee.find(q).populate('department','name code').populate('userId','name email role active').populate('user','name email role active').sort({[sortBy]:sortOrder}).skip((page-1)*limit).limit(limit),Employee.countDocuments(q)]);ok(res,items,'Employees loaded',{page,limit,total,pages:Math.max(Math.ceil(total/limit),1)})}catch(e){next(e)}};
exports.create=async(req,res,next)=>{try{if(await Employee.exists({email:req.body.email}))throw new AppError('An employee with this email already exists',409,'EMAIL_EXISTS');const employee=await Employee.create({fullName:req.body.fullName,email:req.body.email,phone:req.body.phone,department:req.body.department,designation:req.body.designation,status:req.body.status});if(req.body.userId||req.body.user)await links.link(req.body.userId||req.body.user,employee._id);created(res,await Employee.findById(employee._id).populate('department','name code').populate('userId','name email role active'),'Employee added')}catch(e){next(e)}};
exports.update=async(req,res,next)=>{try{const payload={...req.body};delete payload.user;delete payload.userId;const employee=await Employee.findByIdAndUpdate(req.params.id,payload,{new:true,runValidators:true});if(!employee)throw new AppError('Employee not found',404,'NOT_FOUND');if(req.body.userId||req.body.user)await links.link(req.body.userId||req.body.user,employee._id);else{const linked=employee.userId||employee.user;if(linked)await User.findByIdAndUpdate(linked,{$set:{department:employee.department,employeeId:employee._id,employee:employee._id}})}ok(res,await Employee.findById(employee._id).populate('department','name code').populate('userId','name email role active'),'Employee updated')}catch(e){next(e)}};
exports.remove=async(req,res,next)=>{try{const employee=await Employee.findByIdAndDelete(req.params.id);if(!employee)throw new AppError('Employee not found',404,'NOT_FOUND');await User.updateMany({$or:[{employeeId:employee._id},{employee:employee._id}]},{$set:{employeeId:null,employee:null,department:null}});ok(res,null,'Employee deleted')}catch(e){next(e)}};
