const ActivityLog=require('../models/ActivityLog');const User=require('../models/User');const Visitor=require('../models/Visitor');const AppError=require('../utils/appError');const{ok}=require('../utils/response');const escape=value=>String(value).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
exports.list=async(req,res,next)=>{try{const page=Math.max(Number(req.query.page)||1,1),limit=Math.min(Math.max(Number(req.query.limit)||20,1),100),scope=req.user.role==='administrator'?{}:{performedBy:req.user._id},filter={...scope};if(req.query.action)filter.action=req.query.action;if(req.query.role&&req.user.role==='administrator')filter.role=req.query.role;if(req.query.from||req.query.to){filter.createdAt={};if(req.query.from)filter.createdAt.$gte=new Date(`${req.query.from}T00:00:00`);if(req.query.to)filter.createdAt.$lte=new Date(`${req.query.to}T23:59:59.999`);if(filter.createdAt.$gte&&filter.createdAt.$lte&&filter.createdAt.$gte>filter.createdAt.$lte)throw new AppError('Start date must not be after end date',422,'INVALID_DATE_RANGE')}if(req.query.search){const pattern=new RegExp(escape(req.query.search.trim()),'i'),[users,visitors]=await Promise.all([User.find({$or:[{name:pattern},{email:pattern}]}).select('_id').limit(100),Visitor.find({$or:[{visitorName:pattern},{companyName:pattern}]}).select('_id').limit(100)]);filter.$or=[{performedBy:{$in:users.map(x=>x._id)}},{visitor:{$in:visitors.map(x=>x._id)}},{remarks:pattern},{'metadata.targetName':pattern},{'metadata.targetEmail':pattern}]}const[items,total,facets]=await Promise.all([ActivityLog.find(filter).populate('performedBy','name email role').populate('visitor','visitorName companyName status').sort({createdAt:-1}).skip((page-1)*limit).limit(limit).lean(),ActivityLog.countDocuments(filter),ActivityLog.aggregate([{$match:scope},{$group:{_id:'$action',count:{$sum:1}}},{$sort:{count:-1}}])]);ok(res,items,'Activity history loaded',{page,limit,total,pages:Math.max(Math.ceil(total/limit),1),actionCounts:Object.fromEntries(facets.map(x=>[x._id,x.count]))})}catch(error){next(error)}};
