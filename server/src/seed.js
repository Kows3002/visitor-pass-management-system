require('dotenv').config();const connect=require('./config/db');const User=require('./models/User');const Department=require('./models/Department');(async()=>{await connect();for(const[name,code]of[['Engineering','ENG'],['Operations','OPS'],['Finance','FIN'],['Human Resources','HR']])await Department.findOneAndUpdate({code},{name,code,active:true},{upsert:true,new:true,setDefaultsOnInsert:true});const email='admin@visitorpass.com';if(!await User.exists({email}))await User.create({name:'System Administrator',email,password:'Admin@123',role:'administrator',active:true});console.log('Required departments and System Administrator are available.');process.exit(0)})().catch(e=>{console.error(e);process.exit(1)});
