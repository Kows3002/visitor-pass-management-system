const router=require('express').Router();const{query}=require('express-validator');const controller=require('../controllers/activityController');const validate=require('../middleware/validate');const{authenticateUser}=require('../middleware/auth');const actions=['created','approved','rejected','cancelled','checked_in','checked_out','remarks_added','login','password_changed','user_created','user_deleted'];router.get('/',authenticateUser,[query('action').optional({checkFalsy:true}).isIn(actions),query('role').optional({checkFalsy:true}).isIn(['administrator','receptionist','employee']),query('from').optional({checkFalsy:true}).isISO8601({strict:true}),query('to').optional({checkFalsy:true}).isISO8601({strict:true}),query('page').optional({checkFalsy:true}).isInt({min:1}),query('limit').optional({checkFalsy:true}).isInt({min:1,max:100}),query('search').optional({checkFalsy:true}).trim().isLength({max:100})],validate,controller.list);module.exports=router;
