const router=require('express').Router();const{body}=require('express-validator');const controller=require('../controllers/authController');const validate=require('../middleware/validate');const{authenticateUser}=require('../middleware/auth');const rateLimit=require('../middleware/rateLimit');
router.post('/login',rateLimit(),[body('email').trim().normalizeEmail().isEmail().withMessage('Enter a valid email address'),body('password').isString().isLength({min:8,max:128}).withMessage('Password must contain between 8 and 128 characters'),body('rememberMe').optional().isBoolean()],validate,controller.login);
router.post('/logout',authenticateUser,controller.logout);router.get('/profile',authenticateUser,controller.profile);
router.post('/change-password',authenticateUser,[body('currentPassword').isString().notEmpty().withMessage('Current password is required'),body('newPassword').isString().isStrongPassword({minLength:8,minLowercase:1,minUppercase:1,minNumbers:1,minSymbols:1}).withMessage('New password must include uppercase, lowercase, number, and symbol'),body('confirmPassword').isString().custom((value,{req})=>value===req.body.newPassword).withMessage('Passwords do not match')],validate,controller.changePassword);
module.exports=router;
