const router=require('express').Router();const{body,param}=require('express-validator');const c=require('../controllers/visitorController');const validate=require('../middleware/validate');const upload=require('../middleware/upload');const{authenticateUser,authorizeRoles}=require('../middleware/auth');const id=[param('id').isMongoId()];router.use(authenticateUser);router.get('/',c.list);router.get('/:id',id,validate,c.detail);router.get('/:id/history',id,validate,c.history);router.post('/',authorizeRoles('receptionist','administrator'),upload.single('photo'),[body('visitorName').trim().isLength({min:2,max:100}),body('phone').trim().isMobilePhone('any'),body('email').optional({checkFalsy:true}).isEmail(),body('companyName').optional({checkFalsy:true}).trim().isLength({max:150}),body('governmentId').trim().isLength({min:3,max:100}),body('purpose').trim().isLength({min:3,max:300}),body('employee').isMongoId(),body('visitDate').isISO8601({strict:true}),body('expectedArrival').matches(/^([01]\d|2[0-3]):[0-5]\d$/),body('expectedDeparture').matches(/^([01]\d|2[0-3]):[0-5]\d$/)],validate,c.create);router.post('/:id/remarks',authorizeRoles('employee','administrator'),[...id,body('remarks').trim().isLength({min:2,max:1000})],validate,c.remark);router.post('/:id/approve',authorizeRoles('employee','administrator'),[...id,body('remarks').optional({checkFalsy:true}).trim().isLength({max:1000})],validate,c.action('approve'));router.post('/:id/reject',authorizeRoles('employee','administrator'),[...id,body('remarks').trim().isLength({min:2,max:1000})],validate,c.action('reject'));router.post('/:id/cancel',authorizeRoles('receptionist','administrator'),[...id,body('remarks').trim().isLength({min:2,max:1000})],validate,c.action('cancel'));router.post('/:id/checkin',authorizeRoles('receptionist'),id,validate,c.action('checkin'));router.post('/:id/checkout',authorizeRoles('receptionist'),id,validate,c.action('checkout'));module.exports=router;
